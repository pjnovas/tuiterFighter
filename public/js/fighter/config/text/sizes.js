var fighter = fighter || {};
fighter.config = fighter.config || {};
fighter.config.text = fighter.config.text || {};

fighter.config.text.sizes = {

	hitNbo: {
		width: 60,
		height: 60
	},
	hits: {
		width: 60,
		height: 60
	}
};