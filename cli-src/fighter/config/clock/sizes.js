var fighter = fighter || {};
fighter.config = fighter.config || {};
fighter.config.clock = fighter.config.clock || {};

fighter.config.clock.sizes = {

	sphere: {
		width: 100,
		height: 100
	},
	number: {
		width: 40,
		height: 50
	},
	legend: {
		width: 110,
		height: 25
	},
	time: {
		width: 50,
		height: 20
	}
};